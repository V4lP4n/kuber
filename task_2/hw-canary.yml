apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: hw-app
  name: hw-canary
spec:
  replicas: 3
  selector:
    matchLabels:
      app: hw-app
  template:
    metadata:
      labels:
        app: hw-app
        version: "1.0"
    spec:
      containers:
      - image: nginx:latest
        name: nginx
        ports:
        - containerPort: 80
        volumeMounts:
          - name: config-nginx2
            mountPath: /etc/nginx/conf.d
      volumes:
        - name: config-nginx2
          configMap:
            name: nginx-configmap2
        